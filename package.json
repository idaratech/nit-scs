{
  "name": "nit-scs-v2",
  "private": true,
  "version": "1.0.0",
  "description": "NIT Supply Chain System V2 - Full Stack Monorepo",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:backend": "pnpm --filter @nit-scs-v2/backend dev",
    "dev:frontend": "pnpm --filter @nit-scs-v2/frontend dev",
    "build": "pnpm -r run build",
    "build:shared": "pnpm --filter @nit-scs-v2/shared build",
    "build:backend": "pnpm --filter @nit-scs-v2/backend build",
    "build:frontend": "pnpm --filter @nit-scs-v2/frontend build",
    "db:migrate": "pnpm --filter @nit-scs-v2/backend prisma:migrate",
    "db:push": "pnpm --filter @nit-scs-v2/backend prisma:push",
    "db:seed": "pnpm --filter @nit-scs-v2/backend prisma:seed",
    "db:studio": "pnpm --filter @nit-scs-v2/backend prisma:studio",
    "lint": "pnpm -r run lint",
    "format": "prettier --write \"packages/*/src/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"packages/*/src/**/*.{ts,tsx}\"",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean && rm -rf node_modules",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "@eslint/js": "^10.0.1",
    "@faker-js/faker": "^10.3.0",
    "@types/supertest": "^6.0.3",
    "eslint": "^10.0.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "supertest": "^7.2.2",
    "typescript-eslint": "^8.54.0",
    "vitest": "^4.0.18"
  }
}
